---
name: "galaxy42-windows"
sudo: true
enable_cache: true
suites:
- "xenial"
architectures:
- "amd64"
packages:
- "g++-5"
- "mingw-w64"
- "g++-mingw-w64"
- "cmake"
- "autoconf"
- "libtool"
- "build-essential"
- "libfftw3-dev"
- "ca-certificates"  #  because of: Problem with the SSL CA cert (path? access rights?)
- "faketime" # used to force date
reference_datetime: "2016-08-01 00:00:00"
remotes:
- "dir": "galaxy42"
  "url": "https://github.com/damianbb/galaxy42.git" # (username) must be same in: /build-gitian and in main YML file (which names is in $gitian_yml_file)
  "commit": "fc82904ddb45fb0e7e59cd6423ec767d7f18a47d" # um, update that in one more commit, can't commit own commit hash :)
# this is temporary, TODO, in the end use branch name / tag name
files: []
script: |
  datenow="$(date)" ; echo "Gitian moves to the Script section, in PWD=$PWD, at date=$datenow" ; datenow=""
  echo "ls:" ; ls
  ./galaxy42/contrib/gitian-descriptors/main-build/galaxy-windows-script
  datenow="$(date)" ; echo "Gitian finished it's Script section, in PWD=$PWD, at date=$datenow" ; datenow=""

